#include "Stack2.h"

int main(){
    STACK s;
    intialize(s);
    //để các phần tử vào s
    push(s.10);
    push(s.8);
    push(s.5);
    push(s.9);
    push(s.1);
    //lấy các phần tử từ s
    DATA value;
    while (pop(s.value))
    {
        printf("%d\n", value);
    }
    return 0;
    
}